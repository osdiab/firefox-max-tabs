{
  "name": "webextension-max-tabs",
  "version": "0.0.1",
  "description": "Set the maximum number of tabs that can be open",
  "main": "src/index.js",
  "scripts": {
    "build-source": "webpack",
    "test": "jest",
    "lint": "tslint -p . && web-ext lint",
    "prebuild": "npm test && npm run-script lint && npm run-script build-source",
    "prestart": "npm run-script build-source",
    "start": "web-ext run",
    "build": "npm run-script prebuild && web-ext build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/osdiab/webextension-max-tabs.git"
  },
  "keywords": [
    "webextensions",
    "firefox",
    "extension",
    "browser",
    "tabs"
  ],
  "author": "Omar Diab",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/osdiab/webextension-max-tabs/issues"
  },
  "homepage": "https://github.com/osdiab/webextension-max-tabs#readme",
  "dependencies": {
    "ts-loader": "^2.3.3",
    "typescript": "^2.4.2"
  },
  "devDependencies": {
    "@types/jest": "^20.0.7",
    "jest": "^20.0.4",
    "tslint": "^5.6.0",
    "tslint-eslint-rules": "^4.1.1",
    "web-ext": "^2.0.0",
    "webpack": "^3.5.5"
  },
  "jest": {
    "moduleFileExtensions": [
      "ts",
      "js",
      "json"
    ],
    "transform": {
      "^.+\\.(ts|tsx)$": "<rootDir>/scripts/tsc-jest-preprocessor.js"
    },
    "testMatch": [
      "**/?(*.)(spec|test).ts"
    ]
  }
}
